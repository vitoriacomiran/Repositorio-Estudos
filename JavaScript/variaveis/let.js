// declarar variável sem valor
let user
console.log("Rodrigo Gonçalves")

/* a diferença para o var é:
o let não deixa criar mais de uma vez a mesma variável, subscrever.
Diferente do var que deixa eu criar mais uma variável chamada user.

Nesse caso o let melhora a consistência do código */
