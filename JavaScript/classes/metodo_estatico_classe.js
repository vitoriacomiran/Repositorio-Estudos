class User {
    static showMessage(){
        console.log("Essa é uma mensagem")
    }
}
const user = new User()
user.showMessage()

// metodos estáticos são acessados sem instanciar as classes (construtor)

