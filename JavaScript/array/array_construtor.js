// criando um array com construtor

const newArray = new Array()
console.log(newArray) // retorna apenas [] pois o array está vazio
console.log(newArray.length) // retorna 0, pois não tem nada
// [] - Array
// {} - Object

const available = new Array(10) // determino as posições
console.log(available) // retorna vazia
console.log(available.length) // retorna 10 pois são as posições criadas porém vazias


